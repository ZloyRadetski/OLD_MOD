package net.portalmod.common.sorted.portal;

import net.minecraft.world.server.ServerWorld;
import net.minecraftforge.common.world.ForgeChunkManager;

public class PortalChunkLoader implements ForgeChunkManager.LoadingValidationCallback {
   public void validateTickets(ServerWorld level, ForgeChunkManager.TicketHelper ticketHelper) {
   }
}
